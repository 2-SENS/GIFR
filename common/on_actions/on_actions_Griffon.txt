on_actions = {
	on_annex = {
		effect = {
			if = {
				limit = {
					FROM = { tag = TRM }
				}
				every_state = {
					limit = { is_claim_or_core_of_FROM = yes }
					remove_claim_by = FROM
					remove_core_of = FROM
				}
			}
		}
	}
}